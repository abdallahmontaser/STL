#include<iostream>
#include<string>
#include<algorithm>
#include<cmath>
#include<iomanip>
#include<cstdlib>
#include<conio.h>
#include<stdlib.h>
#include<queue>
#include<deque>
#include<stack>
#include<vector>
using namespace std;
#define ll long long
void print(vector<int>& ve)
{
	vector<int>::iterator it;
	it = ve.begin();
	while (it != ve.end())
	{
		cout << *it << " ";
		it++;
	}
}
int main()
{
	vector<int>ve(5);
	for (int i = 0; i < (int)ve.size(); i++)
	{
		ve[i] = i;
	}
	print(ve);
}
